When running from the console without a display manager (e.g. startx),
PAM configuration should be set to auto-unlock the gnome-keyring, as in:
https://wiki.archlinux.org/title/GNOME/Keyring#PAM_step

